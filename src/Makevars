PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

C_OBJS = fortranwithr_init.o
FT_OBJS = fortranwithr.o

all:
	@$(MAKE) $(SHLIB)
	@rm -f *.mod *.o

$(SHLIB): $(FT_OBJS) $(C_OBJS)

fortranwithr_init.o: fortranwithr.o
